# <Type name> (e.g. Runbooks)

<!--
Skeleton for a docs/<type>/README.md — the page that introduces ONE doc type and indexes its docs.
Copy to docs/<type>/README.md, rename to the type, delete these comments. For a single doc use
doc-template.md. The section order below is fixed so every type README reads the same way.
-->

<One short paragraph: what this doc type is and how to tell it apart from the other types in this project.
Keep it to this type; the full cross-type comparison lives in ../README.md, don't repeat it.>

## Index

| Doc                  | Status     | Owner | What it is              |
|----------------------|------------|-------|-------------------------|
| [<title>](<file>.md) | `<status>` | Name  | one-line summary of it. |

## Status lifecycle (required)

Every doc opens with the **shared header** — the same field set, names, and order across all `docs/` types
(see [`../templates/doc-template.md`](../templates/doc-template.md)). Don't repeat the header here; only the
`Status` vocabulary is this type's own:

| Status    | Meaning                             |
|-----------|-------------------------------------|
| `<value>` | <when a doc of this type is in it>. |

## <Other type-specific sections>

<!-- e.g. ADR `Conventions` (filename/numbering rules); omit this section entirely if the type has none. -->
